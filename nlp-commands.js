// Generated by CoffeeScript 1.12.4
var colors, initCommands;

colors = require('colors');

initCommands = function(program) {
  return program.command('nlp').description('Natural Language Processing Utilities').option('-d, --detect-lang <text>', 'Detect language').action(function(options) {
    var NLP;
    NLP = require('./nlp');
    if (options.detectLang) {
      return NLP.detectLanguage({
        text: options.detectLang
      }).then(console.log);
    }
  });
};

module.exports = initCommands;
